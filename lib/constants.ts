/** Bumped whenever the persisted shape changes, so old payloads are ignored. */
export const STORAGE_KEY = "roomie-workspace-v2";

export const LOCATIONS = ["Bali", "Jakarta", "Surabaya"];

export const ALERT_TIMEOUT_MS = 5200;
